apiVersion: v1
kind: Service
metadata:
  name: anime-rec-service
  namespace: default
  labels:
    app.kubernetes.io/name: anime-rec-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: anime-rec-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8501
